# TODO

- [ ] Authenticate with Path of Exile servers
- [x] Make any trade API call
- [x] Abstract the ability to make trade calls to a class with well defined types
- [x] Show the result of the trade calls in a list with basic information
- [ ] Define a combination of multiple input items and an output item with a ratio and their price, to see if buying the input items to create the output item is worth it (hardcoded at first)
- [ ] Create a UI for input and output items that allows adding a link to a POE trade page and that can derive the underlying API call from it
- [ ] Improve how results are displayed in the UI
